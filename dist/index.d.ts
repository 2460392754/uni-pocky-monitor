import { IOpts } from './interface';
export default class {
    private data;
    constructor(opts: IOpts);
    start(): void;
    /** 设置参数 */
    private _setOptions;
    private _performance;
    private _system;
    private _location;
}
